export default (state = [{subtitle:'first subtitle',start:0,end:5},
                            {subtitle:'second subtitle',start:5,end:300}],action)=> {
    switch (action.type){
        default : return state
    }
}